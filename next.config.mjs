/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     remotePatterns: [
    //       {
    //         protocol:'https', // this means only took photos url https which enhance safetry in this case no need cuz we use photo locl folder of /public
    //         hostname: cdn.example.com, // this give when the file come from external url next js <Imgaes /> components can optimize the image for our app
    //       }
    //     ]
    // }
    // experimental: {
    //     serverActions:true,
    // },
};

export default nextConfig ;
